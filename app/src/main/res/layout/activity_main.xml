<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
    <import type="com.example.filtercounter.DataBindingViewsUtil"/>
        <variable
            name="mFilter"
            type="com.example.filtercounter.Filter" />
    </data>

    <LinearLayout

        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:id="@+id/filterResource"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{String.valueOf(mFilter.resource)}" />

        <TextView
            android:id="@+id/filterPassed"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{String.valueOf(mFilter.passed)}" />

        <TextView
            android:id="@+id/filterOneFill"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{String.valueOf(mFilter.oneFill)}" />

    </LinearLayout>

</layout>